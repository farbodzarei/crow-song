# Crow Song Yoga Therapy — project context

Premium, awards-level marketing site for **Crow Song Yoga Therapy** (christine white — therapeutic Ashtanga, Vancouver). Kodax Studio client build. Mood: calm, grounded, quietly mystical — "like an exhale."

## Stack & commands
- **Vite + React + Motion** (`motion/react`), CSS Modules. Single source of truth: `src/styles/tokens.css`.
- Dev: `npm run dev -- --port 5180` · Build: `npm run build`.
- ⚠️ npm cache is redirected via project `.npmrc` to `~/.npm-crowsong` (global `~/.npm` has root-owned files). Installs work via the project dir automatically.
- **GitHub:** `git@github.com:farbodzarei/crow-song.git` (private, branch `main`, SSH). Commit + `git push` as work progresses.
- **Deployed on Vercel** (scope `kodax-studio`, project `crow-song-yoga`). Live: **https://crow-song-yoga.vercel.app**. Redeploy with `npx vercel --prod` (CLI-linked via `.vercel/`, gitignored). Real domain crowsongyogatherapy.com not yet wired. `.npmrc` is gitignored (local cache path breaks CI). GitHub→Vercel auto-deploy not yet connected.
- Screenshots: `puppeteer-core` is installed; system Chrome at `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`. Headless `fullPage` shows a *false* "duplicate page" artifact — ignore it (DOM is fine). Capture ≤2000px images or the reader rejects them.

## Brand rules (FIXED — brand deck in `brand/` is source of truth)
- **Backgrounds: ONLY Crow `#2E2633` (dark) + Haze `#E8E1EE` (light).** Nothing else. (Haze reads near-white but is `#E8E1EE`.)
- **Lavender `#9B8BB8` = accent only** (buttons, dividers, crow, hover, dots). Never a large fill.
- **Raleway only**, with custom **ss09** stylistic set (alternate W) enabled globally in `global.css`.
- **christine** always lowercase. **Crow Song** two caps. Ashtanga-specific (Yoga Chikitsa, Mysore, ujjayi), not generic yoga.
- Slightly rounded corners everywhere (`--radius` 14 / `--radius-sm` 8).
- **One shared full-width layout container** — everything aligns to the same ~60px gutters (`--container` 2240, `--container-pad`).

## What's built
14 sections (Hero, Insight, IntroBand, Approach, Practice, About, Process, Video + Written testimonials, Courses+Membership, Pricing, FAQ, CTA, Footer) + Nav (mega-menu + mobile overlay) + Footer.
Signature/motion: first-load **Intro curtain** (`components/Intro.jsx`), **Lenis smooth scroll**, **magnetic CTAs**, hero **line-mask reveal**, geometric **crow** (`components/Crow.jsx`, source `brand/source-svg/cs_crow_geometric.svg`) with draw-in/breathe/aura/shimmer + a scroll-driven **"take flight"** signature (wings articulate open from their shoulder pivots, the mark rises/scales/fades as you leave the hero — wings & flight-lines are grouped `<motion.g>` with `transform-box: view-box`; reduced-motion renders it static), custom **cursor**, breath **particles**. Logos in `public/logo/` (light on dark, dark on light).
Everything is reduced-motion safe. Always re-verify with a production build + screenshots after changes.

## Signature ornament: mandalas + atmospheric transition
- **`components/Mandala.jsx`** — the client's ornate mandala (`public/mandala.svg`) used as a **CSS mask** (so it's recolourable); a **rotating conic-gradient shimmer** sweeps lavender light through it; it **forms on scroll** (clip-path circle bloom) and is **fully visible at the corner (not cut off)**. `tone="onDark|onLight"`, `side="left|right"`, reduced-motion static. **Host section needs `position:relative; isolation:isolate; overflow:hidden`** (isolate so the `z-index:-1` mandala renders above the section bg, below content). Placed alternating L/R on: Approach(R), About(L), VideoTestimonials(R), Courses(L), Pricing(R). Skipped CTA (already has crow motif). Regenerate-free: swap the SVG file to change the figure.
- **Hero crow** draw-in waits for the loading curtain (`signalIntroDone()` / `cs-intro-done` event + `window.__csIntroDone` flag in `Intro.jsx`), then **loops** (draw → hold 3s → fade → redraw) via `useAnimationControls` in `Crow.jsx`.
- **No horizontal scroll**: `body { overflow-x: clip }` is the global safety net; watch for full-bleed decorative layers (the Insight `.bloom` once overflowed at 130% width — keep such layers ≤100% or clipped by the host).
- **Atmospheric dark→light transition** in `Insight` — a scroll-reactive lavender "dawn bloom" (`.bloom`, Motion opacity/y via `useScroll`) + dark→haze melt (`::after`). The page's first exhale from crow to haze.

## Mobile / native-iOS layer
The mobile build is tuned to feel like a standalone native app:
- **PWA / standalone**: `manifest.webmanifest` + apple-mobile-web-app meta (`black-translucent` status bar), `viewport-fit=cover`. On-brand crow app icons in `public/` (`icon-192/512.png`, `apple-touch-icon.png`, `favicon.svg`) are generated by `gen-icons.mjs` from the crow geometry — rerun it if the mark changes. (The old `favicon.svg` was an off-brand purple mark; replaced.)
- **Safe-area insets** (`env(safe-area-inset-*)`) on nav top, mobile menu, footer bottom, hero scroll hint, and the bottom bar. `.container` uses `max(gutter, inset-left/right)` so non-notch devices are unchanged.
- **`components/MobileBar.jsx`** — persistent thumb-reachable bottom action bar (free call · book), blurred + safe-area aware, mobile-only (`≤960px`); slides up past the hero, tucks away when `#contact`/footer is in view.
- **Native touch hygiene** (global.css): `overscroll-behavior-y: contain` (no pull-to-refresh), instant `:active` press-scale on every `.cursor-target` gated to `(hover:none) and (pointer:coarse)`, `-webkit-touch-callout`/`user-select:none` on controls, `touch-action: manipulation`.

## SEO / share-readiness
- index.html: Open Graph + Twitter card meta, canonical, and JSON-LD `LocalBusiness` (founder christine white, Vancouver, offers; **telephone is placeholder**).
- `public/og.png` (1200×630 crow share card) generated by `gen-og.mjs` (needs the preview server running for fonts; rerun if the wordmark/tagline changes).
- `public/sitemap.xml` + `public/robots.txt` (point at the intended domain crowsongyogatherapy.com).

## Open placeholders (need christine's real info before launch)
Real prices (pricing/courses/membership are sample), real testimonials, booking method (`#book` placeholder), phone number, christine's portrait + bio facts, final domain (using crowsongyogatherapy.com).

## Working style
Act autonomously — don't ask "want me to proceed?" unless truly critical. Keep answers short. The imported Claude-Design HTML was only a direction snapshot — ignore it; the brand deck + direct instructions win.

For an awards-level elevation, act as a senior creative director and apply the **frontend-design** skill philosophy: spend boldness in ONE signature place, keep everything else disciplined.
